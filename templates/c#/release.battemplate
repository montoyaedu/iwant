@echo on

rm -fr target                                                                   

git clone --branch %1 ${Remote}:${Owner}/${ProjectName}.git target   
if ERRORLEVEL 1 goto Error

cd target                                                                       
if ERRORLEVEL 1 goto Error

call buildandpublish
if ERRORLEVEL 1 goto Error

goto:eof

:Error
echo Error. Verify Console Output.

goto:eof
